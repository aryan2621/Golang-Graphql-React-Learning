package main

import "fmt"

func main()  {
	// go is case sensitive
	//exported functions are defined inside the go and they are defined as the capital letter start
	// if you are using the inbuilt function make sure they are define and used inside with capital letters 
	
}